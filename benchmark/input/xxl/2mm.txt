alpha = 1.5
beta = 1.2

∀(i) | 0<=i<6000 {
    ∀(j) | 0<=j<=7000 {
        temp[i][j] = Σ(alpha * a[i][k] * b[k][j]) | 0<=k<=8000
    }
}

∀(i) | 0<=i<=6000 {
    ∀(j) | 0<=j<=9000 {
        d[i][j] = d[i][j] * beta
        d[i][j] = Σ(temp[i][k] * c[k][j]) | 0<=k<=7000
    }
}
